# CF-Helper 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现单文件 bash 脚本 `cf-helper.sh`，通过 Cloudflare API 管理域名的 Under Attack 模式和 Development 模式。

**Architecture:** 单文件脚本，分为凭证检查、API 封装层、功能函数、命令行解析、交互菜单五个逻辑区块，顺序执行。依赖 `curl` + `jq`，无其他依赖。

**Tech Stack:** bash, curl, jq, Cloudflare REST API v4

## Global Constraints

- 目标文件：`cf-helper.sh`（项目根目录）
- 依赖：`curl`、`jq`，缺失时报错并提示安装命令，退出码 1
- 凭证：仅从环境变量 `CF_API_TOKEN` 读取，不写入文件
- CF API 基础 URL：`https://api.cloudflare.com/client/v4`
- 颜色输出：检测 `$TERM != dumb` 且 stdout 是终端（`-t 1`），否则降级纯文本
- 关闭 Under Attack 还原值：`high`（非 `medium`）
- 批量操作关键字：`all`
- 脚本需 `chmod +x` 后可直接运行

---

### Task 1: 脚手架 + 凭证检查 + 依赖检查

**Files:**
- Create: `cf-helper.sh`

**Interfaces:**
- Produces:
  - 全局变量 `CF_API_BASE="https://api.cloudflare.com/client/v4"`
  - 颜色变量 `RED GREEN YELLOW NC`（无终端时为空字符串）
  - 函数 `log_info <msg>`、`log_ok <msg>`、`log_err <msg>` — 带颜色前缀输出到 stdout/stderr
  - 函数 `check_deps` — 检查 curl/jq，缺失则报错退出
  - 函数 `check_token` — 检查 CF_API_TOKEN，缺失则报错退出

- [ ] **Step 1: 创建脚本文件并写入 shebang + 全局变量**

```bash
#!/usr/bin/env bash
set -euo pipefail

CF_API_BASE="https://api.cloudflare.com/client/v4"

# 颜色支持检测
if [[ -t 1 && "${TERM:-}" != "dumb" ]]; then
  RED='\033[0;31m'
  GREEN='\033[0;32m'
  YELLOW='\033[0;33m'
  NC='\033[0m'
else
  RED='' GREEN='' YELLOW='' NC=''
fi
```

- [ ] **Step 2: 写入日志函数**

```bash
log_info() { echo -e "${YELLOW}[INFO]${NC} $*"; }
log_ok()   { echo -e "${GREEN}[OK]${NC}   $*"; }
log_err()  { echo -e "${RED}[ERROR]${NC} $*" >&2; }
```

- [ ] **Step 3: 写入依赖检查函数**

```bash
check_deps() {
  local missing=0
  for cmd in curl jq; do
    if ! command -v "$cmd" &>/dev/null; then
      log_err "缺少依赖：$cmd"
      case "$cmd" in
        jq)   log_err "  安装：apt install jq  /  brew install jq  /  yum install jq" ;;
        curl) log_err "  安装：apt install curl  /  brew install curl" ;;
      esac
      missing=1
    fi
  done
  [[ $missing -eq 0 ]] || exit 1
}
```

- [ ] **Step 4: 写入凭证检查函数**

```bash
check_token() {
  if [[ -z "${CF_API_TOKEN:-}" ]]; then
    log_err "未找到 CF_API_TOKEN，请先执行："
    log_err "  export CF_API_TOKEN=\"your_api_token\""
    exit 1
  fi
}
```

- [ ] **Step 5: 手动验证凭证检查行为**

```bash
# 测试缺失 token
bash cf-helper.sh 2>&1 | grep "CF_API_TOKEN"
# 预期输出包含：未找到 CF_API_TOKEN

# 测试缺失依赖（临时改名 jq 测试，验证后还原）
```

- [ ] **Step 6: 提交**

```bash
git add cf-helper.sh
git commit -m "feat: 脚本脚手架、凭证检查、依赖检查"
```

---

### Task 2: API 封装层

**Files:**
- Modify: `cf-helper.sh`

**Interfaces:**
- Consumes: `CF_API_BASE`, `CF_API_TOKEN`, `log_err`
- Produces:
  - 函数 `cf_api <method> <path> [json_body]` → stdout 输出原始 JSON 响应，HTTP 错误时 stderr 报错并返回非零退出码
  - 函数 `cf_check_success <json>` → 检查 `.success` 字段，失败时打印 `.errors[].message` 并返回 1

- [ ] **Step 1: 写入 `cf_api` 函数**

```bash
cf_api() {
  local method="$1"
  local path="$2"
  local body="${3:-}"
  local args=(-s -w "\n%{http_code}" -X "$method"
    -H "Authorization: Bearer ${CF_API_TOKEN}"
    -H "Content-Type: application/json"
    "${CF_API_BASE}${path}")
  [[ -n "$body" ]] && args+=(-d "$body")

  local response http_code
  response=$(curl "${args[@]}")
  http_code=$(tail -n1 <<< "$response")
  response=$(head -n -1 <<< "$response")

  if [[ "$http_code" -lt 200 || "$http_code" -ge 300 ]]; then
    log_err "HTTP $http_code：$(jq -r '.errors[0].message // "未知错误"' <<< "$response")"
    return 1
  fi
  echo "$response"
}
```

- [ ] **Step 2: 写入 `cf_check_success` 函数**

```bash
cf_check_success() {
  local json="$1"
  if [[ "$(jq -r '.success' <<< "$json")" != "true" ]]; then
    local msg
    msg=$(jq -r '.errors[0].message // "未知错误"' <<< "$json")
    log_err "API 返回失败：$msg"
    return 1
  fi
}
```

- [ ] **Step 3: 手动验证（需要有效 token）**

```bash
export CF_API_TOKEN="your_token"
# 调用一个会失败的路径，验证错误输出
bash -c 'source cf-helper.sh; cf_api GET /zones/invalid-id' 2>&1
# 预期：[ERROR] HTTP 400 或 404 + 错误信息
```

- [ ] **Step 4: 提交**

```bash
git add cf-helper.sh
git commit -m "feat: CF API 封装层"
```

---

### Task 3: 功能函数（get_zones / set_security_level / set_dev_mode）

**Files:**
- Modify: `cf-helper.sh`

**Interfaces:**
- Consumes: `cf_api`, `cf_check_success`, `log_info`, `log_ok`, `log_err`
- Produces:
  - 函数 `get_zones` → stdout 输出 JSON 数组 `[{"name":"...","id":"..."},...]`
  - 函数 `get_zone_id <zone_name>` → stdout 输出 zone_id 字符串，未找到返回 1
  - 函数 `set_security_level <zone_id> <zone_name> <under_attack|high>` → 执行并打印结果
  - 函数 `set_dev_mode <zone_id> <zone_name> <on|off>` → 执行并打印结果

- [ ] **Step 1: 写入 `get_zones`（支持自动翻页）**

```bash
get_zones() {
  local page=1 all_zones='[]'
  while true; do
    local resp
    resp=$(cf_api GET "/zones?per_page=50&page=${page}") || return 1
    cf_check_success "$resp" || return 1

    local zones total_pages
    zones=$(jq '[.result[] | {name: .name, id: .id}]' <<< "$resp")
    total_pages=$(jq -r '.result_info.total_pages' <<< "$resp")
    all_zones=$(jq -s '.[0] + .[1]' <(echo "$all_zones") <(echo "$zones"))

    [[ "$page" -ge "$total_pages" ]] && break
    ((page++))
  done
  echo "$all_zones"
}
```

- [ ] **Step 2: 写入 `get_zone_id`**

```bash
get_zone_id() {
  local zone_name="$1" zones="$2"
  local zone_id
  zone_id=$(jq -r --arg name "$zone_name" '.[] | select(.name == $name) | .id' <<< "$zones")
  if [[ -z "$zone_id" ]]; then
    log_err "未找到域名：$zone_name"
    return 1
  fi
  echo "$zone_id"
}
```

- [ ] **Step 3: 写入 `set_security_level`**

```bash
set_security_level() {
  local zone_id="$1" zone_name="$2" value="$3"
  local label
  [[ "$value" == "under_attack" ]] && label="Under Attack 模式" || label="高防护模式(high)"
  log_info "正在对 ${zone_name} 设置安全级别 → ${label}..."
  local resp
  resp=$(cf_api PATCH "/zones/${zone_id}/settings/security_level" \
    "{\"value\":\"${value}\"}") || return 1
  cf_check_success "$resp" || return 1
  local current
  current=$(jq -r '.result.value' <<< "$resp")
  log_ok "${zone_name} 安全级别已设为：${current}"
}
```

- [ ] **Step 4: 写入 `set_dev_mode`**

```bash
set_dev_mode() {
  local zone_id="$1" zone_name="$2" value="$3"
  local label
  [[ "$value" == "on" ]] && label="开启" || label="关闭"
  log_info "正在对 ${zone_name} ${label}开发模式..."
  local resp
  resp=$(cf_api PATCH "/zones/${zone_id}/settings/development_mode" \
    "{\"value\":\"${value}\"}") || return 1
  cf_check_success "$resp" || return 1
  local current
  current=$(jq -r '.result.value' <<< "$resp")
  log_ok "${zone_name} 开发模式当前状态：${current}"
}
```

- [ ] **Step 5: 提交**

```bash
git add cf-helper.sh
git commit -m "feat: get_zones / set_security_level / set_dev_mode 功能函数"
```

---

### Task 4: 批量操作辅助函数

**Files:**
- Modify: `cf-helper.sh`

**Interfaces:**
- Consumes: `get_zones`, `get_zone_id`, `set_security_level`, `set_dev_mode`, `log_info`, `log_ok`, `log_err`
- Produces:
  - 函数 `do_action <zone_name|all> <action> <zones_json>` → 执行指定操作，返回成功/失败汇总

- [ ] **Step 1: 写入 `do_action`**

```bash
do_action() {
  local target="$1" action="$2" zones_json="$3"
  local success=0 fail=0

  run_one() {
    local zname="$1" zid="$2"
    case "$action" in
      attack-on)  set_security_level "$zid" "$zname" "under_attack" && ((success++)) || ((fail++)) ;;
      attack-off) set_security_level "$zid" "$zname" "high"         && ((success++)) || ((fail++)) ;;
      dev-on)     set_dev_mode       "$zid" "$zname" "on"           && ((success++)) || ((fail++)) ;;
      dev-off)    set_dev_mode       "$zid" "$zname" "off"          && ((success++)) || ((fail++)) ;;
      *) log_err "未知操作：$action"; return 1 ;;
    esac
  }

  if [[ "$target" == "all" ]]; then
    while IFS= read -r zone; do
      local zname zid
      zname=$(jq -r '.name' <<< "$zone")
      zid=$(jq -r '.id'   <<< "$zone")
      run_one "$zname" "$zid"
    done < <(jq -c '.[]' <<< "$zones_json")
    echo ""
    log_info "批量完成：成功 ${success} 个，失败 ${fail} 个"
    [[ $fail -eq 0 ]] || return 1
  else
    local zid
    zid=$(get_zone_id "$target" "$zones_json") || return 1
    run_one "$target" "$zid"
  fi
}
```

- [ ] **Step 2: 提交**

```bash
git add cf-helper.sh
git commit -m "feat: 批量操作辅助函数 do_action"
```

---

### Task 5: 命令行模式 + 交互菜单 + 入口

**Files:**
- Modify: `cf-helper.sh`

**Interfaces:**
- Consumes: `check_deps`, `check_token`, `get_zones`, `do_action`
- Produces: 完整可运行的脚本入口

- [ ] **Step 1: 写入交互菜单函数**

```bash
interactive_menu() {
  local zones_json="$1"
  local zone_names=()
  while IFS= read -r name; do
    zone_names+=("$name")
  done < <(jq -r '.[].name' <<< "$zones_json")

  echo ""
  echo "=== 域名列表 ==="
  echo "  [0] 所有域名"
  local i=1
  for name in "${zone_names[@]}"; do
    printf "  [%d] %s\n" "$i" "$name"
    ((i++))
  done
  echo ""
  read -rp "选择域名（输入编号）: " choice

  local target
  if [[ "$choice" == "0" ]]; then
    target="all"
  elif [[ "$choice" =~ ^[0-9]+$ ]] && (( choice >= 1 && choice <= ${#zone_names[@]} )); then
    target="${zone_names[$((choice-1))]}"
  else
    log_err "无效选择：$choice"
    exit 1
  fi

  echo ""
  echo "=== 选择操作 ==="
  echo "  [1] 开启 Under Attack 模式"
  echo "  [2] 关闭 Under Attack 模式（还原为 high）"
  echo "  [3] 开启开发模式（Development Mode）"
  echo "  [4] 关闭开发模式"
  echo ""
  read -rp "选择操作（输入编号）: " op_choice

  local action
  case "$op_choice" in
    1) action="attack-on"  ;;
    2) action="attack-off" ;;
    3) action="dev-on"     ;;
    4) action="dev-off"    ;;
    *) log_err "无效操作：$op_choice"; exit 1 ;;
  esac

  echo ""
  do_action "$target" "$action" "$zones_json"
}
```

- [ ] **Step 2: 写入 `usage` 函数**

```bash
usage() {
  cat <<EOF
用法：
  $(basename "$0")                              # 交互菜单
  $(basename "$0") <zone_name|all> <action>     # 直接执行

action 可选值：
  attack-on   开启 Under Attack 模式
  attack-off  关闭 Under Attack 模式（还原为 high）
  dev-on      开启开发模式
  dev-off     关闭开发模式

示例：
  $(basename "$0") example.com attack-on
  $(basename "$0") all dev-off

环境变量：
  CF_API_TOKEN  Cloudflare API Token（必填）
EOF
}
```

- [ ] **Step 3: 写入脚本入口（main 逻辑，放在文件末尾）**

```bash
main() {
  check_deps
  check_token

  if [[ $# -eq 0 ]]; then
    log_info "正在获取域名列表..."
    local zones_json
    zones_json=$(get_zones) || exit 1
    local count
    count=$(jq 'length' <<< "$zones_json")
    log_ok "共找到 ${count} 个域名"
    interactive_menu "$zones_json"
  elif [[ $# -eq 2 ]]; then
    local target="$1" action="$2"
    log_info "正在获取域名列表..."
    local zones_json
    zones_json=$(get_zones) || exit 1
    do_action "$target" "$action" "$zones_json"
  else
    usage
    exit 1
  fi
}

main "$@"
```

- [ ] **Step 4: 设置可执行权限并端到端测试**

```bash
chmod +x cf-helper.sh

# 测试 usage（无 token 时）
unset CF_API_TOKEN
bash cf-helper.sh 2>&1 | grep "CF_API_TOKEN"
# 预期：[ERROR] 未找到 CF_API_TOKEN

# 测试参数数量错误
export CF_API_TOKEN=test
bash cf-helper.sh only-one-arg 2>&1
# 预期：输出 usage 帮助信息

# 测试未知域名（需要有效 token）
bash cf-helper.sh nonexistent.example attack-on 2>&1 | grep "未找到域名"
# 预期：[ERROR] 未找到域名：nonexistent.example
```

- [ ] **Step 5: 提交**

```bash
git add cf-helper.sh
git commit -m "feat: 交互菜单、命令行入口，脚本功能完整"
```

---

## 完成标准

- [ ] `./cf-helper.sh` 无 token 时报错退出，提示 export 命令
- [ ] `./cf-helper.sh` 有 token 时展示域名列表，可交互选择操作
- [ ] `./cf-helper.sh example.com attack-on` 直接执行，打印操作前后状态
- [ ] `./cf-helper.sh all dev-on` 批量操作，最终汇总成功/失败数
- [ ] `./cf-helper.sh bad-arg` 打印 usage 并退出码 1
- [ ] 终端支持颜色时带颜色，重定向时降级纯文本
